<UserControl x:Class="CableWrapMonitor.SequenceItems.UnwindCableInstructionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="400" d:DesignHeight="60">

    <!--
        Compact status line shown for each UnwindCableInstruction in the sequencer.
    -->
    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="4,0">

        <TextBlock Text="Current wrap: "
                   Foreground="{DynamicResource PrimaryBrush}"
                   VerticalAlignment="Center" />

        <TextBlock Text="{Binding TotalDegreesRotatedDisplay}"
                   FontWeight="Bold"
                   Foreground="{DynamicResource PrimaryBrush}"
                   VerticalAlignment="Center" />

        <TextBlock Text=" — will unwind and reset counter"
                   Foreground="{DynamicResource PrimaryBrush}"
                   VerticalAlignment="Center" />

    </StackPanel>
</UserControl>
